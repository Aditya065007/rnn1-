# Core
streamlit>=1.32.0
numpy>=1.24.0

# TensorFlow — pinned to avoid Keras 3 breaking changes
tensorflow==2.15.0

# NLP — pinned to avoid punkt_tab KeyError (NLTK >= 3.8 introduced punkt_tab)
nltk==3.8.1

# Pickle is built-in — no entry needed
scikit-learn>=1.3.0
